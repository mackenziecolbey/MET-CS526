# MET-CS526

CS526 WEEK 1- Hello World
I have not taken any full programming courses, but during my undergraduate studies in Ocean Science I have used and become familiar with RStudio, Python, and MATLAB. I know Python and have used it before but I have only used Java once. I am in my first year of the Data Analytics graduate program. I would classify my experience with algorithms and data structures as beginner. I had some programming and data processing experience during my undergraduate degree, and I've used algorithms for working with scientific data, but I have not had much formal computer science experience with data structures or algorithm design.

This problem asked for a program that takes a file as input and prints the content of the file as output. I created a Python program that takes a file as input and prints the information from the file onto the screen. It takes the contents from the input file, the reads through it line by line, and prints each line. There is nothing that has been changed or added, so the input file and the output files match. The program also works with files that have multiple lines. The input file is called "myfile" and this contains "Hello from David Mellor". The program takes this text from the file and prints "Hello from David Mellor" as the output. I ran the program from the command line by using: Get-Content myfile | python HelloWorld.py. This takes the content from "myfile" and sends it to the Python program and prints it.
